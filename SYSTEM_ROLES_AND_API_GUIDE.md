# دليل النظام الشامل والأدوار الخمسة (School Management System - Roles & API Guide)

تم تطوير وتوسيع هذا النظام ليصبح نظام إدارة مدرسية متكاملاً واحترافياً يخدم **5 أدوار رئيسية (Roles)** مع توفير بيانات أولية غنية وواقعية (Seed Data).

---

## 🔑 1. بيانات تسجيل الدخول الجاهزة (Ready Credentials)

جميع الحسابات تعمل بكلمة المرور الافتراضية الموحدة: `123456` (ما عدا حساب admin كلمة مروره `admin123`).

| الدور (Role) | اسم المستخدم / البريد (Username / Email) | كلمة المرور (Password) | الاسم والشخصية في النظام |
| :--- | :--- | :--- | :--- |
| **المدير (Principal)** | `admin` أو `principal` | `admin123` / `123456` | د. محمد الخطيب (Mohammad Al-Khatib) |
| **أمين السر (Secretary)** | `secretary` | `123456` | أ. سارة الأحمد (Sara Al-Ahmad) |
| **أمين المكتبة (Librarian)** | `librarian` | `123456` | أ. عمر خالد (Omar Khaled) |
| **معلم الرياضيات (Teacher)** | `teacher1` أو `mohammad` | `123456` | أ. محمد صالح (Mohammad Saleh) |
| **معلمة اللغة العربية (Teacher)** | `teacher2` أو `lina` | `123456` | أ. لينا أحمد (Lina Ahmad) |
| **معلم الفيزياء (Teacher)** | `teacher3` أو `khaled` | `123456` | أ. خالد عمر (Khaled Omar) |
| **معلمة اللغة الإنجليزية (Teacher)** | `teacher4` أو `nour` | `123456` | أ. نور علي (Nour Ali) |
| **معلم الكيمياء (Teacher)** | `teacher5` أو `basil` | `123456` | أ. باسل القاسم (Basil Al-Qassem) |
| **معلمة التاريخ (Teacher)** | `teacher6` أو `huda` | `123456` | أ. هدى منصور (Huda Mansour) |
| **طالب (Student - عاشر أ)** | `student1` أو `st1001` | `123456` | أحمد علي (Ahmad Ali) - قيد `ST1001` |
| **طالب (Student - عاشر أ)** | `student2` أو `st1002` | `123456` | محمد حسن (Mohammed Hassan) - قيد `ST1002` |
| **طالبة (Student - عاشر أ)** | `student3` أو `st1003` | `123456` | سارة عمر (Sara Omar) - قيد `ST1003` |
| **طالبة (Student - عاشر ب)** | `student4` أو `st1004` | `123456` | لينا خالد (Lina Khaled) - قيد `ST1004` |
| **طالب (Student - عاشر ب)** | `student5` أو `st1005` | `123456` | يوسف محمود (Yousef Mahmoud) - قيد `ST1005` |
| **طالب (Student - حادي عشر أ)**| `student7` أو `st1101` | `123456` | زيد المصري (Zaid Al-Masri) - قيد `ST1101` |
| **طالب (Student - بكالوريا أ)** | `student9` أو `st1201` | `123456` | طارق الخطيب (Tarek Al-Khatib) - قيد `ST1201` |
| **ولي أمر (Parent / Guardian)** | `guardian1` أو `parent1` | `123456` | م. علي أحمد (أبو أحمد) - ولي أمر أحمد وزيد |
| **ولي أمر (Parent / Guardian)** | `guardian2` أو `parent2` | `123456` | د. منى إبراهيم (أم سارة) - ولي أمر سارة عمر |
| **ولي أمر (Parent / Guardian)** | `guardian3` أو `parent3` | `123456` | أ. خالد المنصور (أبو لينا) - ولي أمر لينا خالد |
| **ولي أمر (Parent / Guardian)** | `guardian4` أو `parent4` | `123456` | السيد محمود القدسي (أبو يوسف) - ولي أمر يوسف |
| **ولي أمر (Parent / Guardian)** | `guardian5` أو `parent5` | `123456` | أ.د. سامي الخطيب (أبو طارق) - ولي أمر طارق |

---

## 🌐 2. تسجيل الدخول والحصول على JWT Token

- **Endpoint**: `POST /api/auth/login`
- **Request Body**:
```json
{
  "email": "admin",
  "password": "admin123"
}
```
- **Response**:
```json
{
  "token": "eyJhbGciOiJIUzI1NiJ9...",
  "role": "PRINCIPAL",
  "email": "admin",
  "refId": 1
}
```

يتم إرسال الـ Token في جميع الطلبات اللاحقة ضمن ترويسة الطلب:
`Authorization: Bearer <TOKEN>`

---

## 🏛️ 3. واجهات المدير (Principal APIs)

| الوظيفة | HTTP Method & URL | الوصف |
| :--- | :--- | :--- |
| **لوحة التحكم والإحصائيات** | `GET /api/principal/dashboard-stats` | إحصائيات عامة (أعداد الطلاب، المعلمين، الصفوف، الحضور، الإيرادات) |
| **الملف الشخصي** | `GET /api/principal/me` | بيانات المدير الحالي |
| **إضافة كادر مدرسي** | `POST /api/principal/create-user` | إضافة معلم أو أمين سر أو أمين مكتبة |
| **إضافة صف دراسي** | `POST /api/principal/create-class` | إنشاء شعبة وصف جديد |
| **تعيين حصة لجدول** | `POST /api/principal/assign-schedule` | إسناد معلم ومادة لحصة في الجدول |
| **إدارة المواد** | `POST / PUT / DELETE /api/subjects/**` | إنشاء وتعديل المواد الدراسية |
| **ربط معلم بمادة** | `POST /api/teacher-subjects/assign/{teacherId}/{subjectId}` | تخويل المعلم لتدريس مادة معينة |
| **إنشاء امتحان رسمي** | `POST /api/exams` | إنشاء جدول امتحان نصفي أو نهائي |
| **تفعيل / تعطيل حساب** | `POST /api/auth/activate-account` / `deactivate-account` | التحكم بحالة الحسابات |

---

## 📝 4. واجهات أمين السر (Secretary APIs)

| الوظيفة | HTTP Method & URL | الوصف |
| :--- | :--- | :--- |
| **الملف الشخصي** | `GET /api/secretary/me` | بيانات أمين السر الحالي |
| **تسجيل طالب جديد** | `POST /api/secretary/student` | إنشاء ملف وحساب لطالب جديد |
| **تسجيل ولي أمر جديد** | `POST /api/secretary/guardian` | إنشاء ملف وحساب لولي أمر |
| **ربط طالب بولي أمر** | `POST /api/student-guardian/connect/{studentId}/{guardianId}` | إنشاء صلة القرابة وتحديد الولي الأساسي |
| **تسكين طالب بصف** | `PUT /api/students/{studentId}/assign-class/{classId}` | نقل أو تسكين الطالب في صف وشعبة |
| **إدارة بيانات الطلاب** | `GET / PUT / DELETE /api/students/**` | استعراض وتحديث بيانات الطلاب |
| **إدارة الحضور والغياب** | `POST / PUT / GET /api/attendance/**` | رصد وتعديل حضور وغياب الطلاب |
| **تسجيل سند قبض مالي** | `POST /api/payments` | تسجيل دفعة مالية وإيصال لطالب |
| **إصدار إنذار سلوكي** | `POST /api/warnings` | تسجيل إنذار أو ملاحظة سلوكية بحق طالب |
| **إدارة رسوم الصفوف** | `POST / PUT / GET /api/class-fees/**` | تحديد رسوم كل مرحلة وصف دراسي |

---

## 👨‍🏫 5. واجهات المعلم (Teacher APIs)

| الوظيفة | HTTP Method & URL | الوصف |
| :--- | :--- | :--- |
| **الملف الشخصي** | `GET /api/teacher/me` | بيانات المعلم وتخصصه |
| **جدول الحصص الأسبوعي** | `GET /api/teacher/my-schedule` | الحصص الأسبوعية الموزعة على الأيام |
| **طلابي المكلف بتدريسهم** | `GET /api/teacher/my-students` | قائمة بجميع الطلاب في الشعب التي يدرسها |
| **تسجيل الحضور للحصة** | `POST /api/attendance` | تسجيل حضور وغياب طلاب الصف |
| **إنشاء تقييم مستمر** | `POST /api/assessments` | إنشاء مذاكرة أو واجب أو نشاط لمادته |
| **رصد درجات التقييم** | `POST /api/assessment-results` | رصد درجات الطلاب في المذاكرة أو النشاط |
| **الامتحانات المكلف بها** | `GET /api/exams/teacher?classId={id}` | الامتحانات الرسمية الخاصة بمادته وصفه |
| **رصد درجات الامتحان** | `POST /api/exam-results` | إدخال وتعديل درجات الامتحان الرسمي للطلاب |
| **إصدار ملاحظة سلوكية** | `POST /api/warnings` | توجيه إنذار أو ملاحظة سلوكية لطالب |

---

## 🎓 6. واجهات الطالب (Student APIs)

| الوظيفة | HTTP Method & URL | الوصف |
| :--- | :--- | :--- |
| **الملف الشخصي** | `GET /api/students/me` | بيانات الطالب، رقم القيد، والصف الحالي |
| **جدول الحصص الأسبوعي** | `GET /api/schedules/my-schedule` | جدول الحصص الخاص بصف الطالب |
| **المواد الدراسية** | `GET /api/students/me-subject` | قائمة المواد المسجلة للفصل الحالي |
| **سجل الحضور والغياب** | `GET /api/students/me-attendance` | كشف الحضور والغياب اليومي |
| **إحصائيات الحضور** | `GET /api/students/attendance/student/{id}/statistics` | عدد أيام الحضور والغياب والتأخير |
| **نتائج التقييمات والمذاكرات** | `GET /api/assessment-results/me` | نتائج المذاكرات والواجبات والمشاريع |
| **جدول الامتحانات** | `GET /api/exams/my-class` | مواعيد الامتحانات الفصلية لصف الطالب |
| **نتائج الامتحانات الرسمية** | `GET /api/exam-results/me` | كشف الدرجات الرسمي للامتحانات الفصلية |
| **الإنذارات والملاحظات** | `GET /api/warnings/me` | أي تنبيهات أو إنذارات موجهة للطالب |
| **سجل المدفوعات والرسوم** | `GET /api/payments/me` | كشف الدفعات والإيصالات المسددة |
| **المكتبة والكتب المستعارة** | `GET /api/book` / `GET /api/borrowed-book` | الكتب المتاحة وسجل استعارة الكتب |
| **الإعلانات المدرسية** | `GET /api/announcements` | الإعلانات والأخبار الموجهة للطلاب |

---

## 👨‍👩‍👧‍👦 7. واجهات ولي الأمر (Parent / Guardian APIs)

| الوظيفة | HTTP Method & URL | الوصف |
| :--- | :--- | :--- |
| **الملف الشخصي** | `GET /api/guardians/me` | بيانات ولي الأمر |
| **قائمة أبنائي المسجلين** | `GET /api/student-guardian/guardian/me` | قائمة بجميع الأبناء التابعين لولي الأمر مع صفوفهم |
| **جدول حصص الابن** | `GET /api/schedules/student/{studentId}` | جدول الحصص الأسبوعي لابنه |
| **سجل حضور وغياب الابن** | `GET /api/attendance/guardian/student/{studentId}` | كشف الحضور والغياب اليومي للابن |
| **إحصائيات حضور الابن** | `GET /api/students/attendance/student/{studentId}/statistics` | نسب الحضور والغياب والأعذار |
| **نتائج تقييمات ومذاكرات الابن**| `GET /api/assessment-results/guardian/student/{studentId}` | درجات المذاكرات والاختبارات القصيرة |
| **جدول امتحانات الابن** | `GET /api/exams/class?classId={classId}&semesterName=FIRST` | مواعيد امتحانات صف الابن |
| **نتائج امتحانات الابن** | `GET /api/exam-results/guardian/student/{studentId}` | كشف الدرجات الرسمي لامتحانات الابن |
| **إنذارات وملاحظات الابن** | `GET /api/warnings/guardian/student/{studentId}` | الملاحظات السلوكية والتنبيهات الموجهة للابن |
| **سجل مدفوعات الابن** | `GET /api/payments/guardian/student/{studentId}` | سجل الأقساط والرسوم المسددة للابن |
| **الإعلانات المدرسية** | `GET /api/announcements` | إعلانات واجتماعات أولياء الأمور والفعاليات |

---

## 📊 8. ملخص هيكل البيانات المزروعة (Seeded Data Summary)

- **المدرسة**: مدرسة النور الدولية النموذجية (Al Noor International Academy) - دمشق، المزة.
- **العام الدراسي والفصول**: العام 2025-2026، الفصل الأول (نشط)، الفصل الثاني.
- **الحسابات الإدارية**: مدير (د. محمد الخطيب)، أمينة سر (أ. سارة الأحمد)، أمين مكتبة (أ. عمر خالد).
- **الهيئة التدريسية**: 6 معلمين (رياضيات، فيزياء، كيمياء، لغة عربية، لغة إنجليزية، تاريخ).
- **الصفوف والشعب**:
  - الصف العاشر: شعبة أ (Room 101)، شعبة ب (Room 102).
  - الصف الحادي عشر: شعبة أ (Room 201)، شعبة ب (Room 202).
  - الصف الثاني عشر: شعبة أ (Room 301).
- **المواد والجدول الأسبوعي**: 15 مادة دراسية موزعة على 5 أيام دراسية من الأحد إلى الخميس بحصص فعلية وربط مع المعلمين.
- **الطلاب**: 10 طلاب بملفات كاملة وأرقام قيد رسمية وحسابات تسجيل دخول.
- **أولياء الأمور**: 5 أولياء أمور بحسابات مستقلة وروابط أسرية موثقة مع أبنائهم.
- **الحضور والغياب**: سجل حضور لـ 7 أيام دراسية متنوعة لجميع الطلاب.
- **التقييمات المستمرة**: مذاكرات، تقارير مخبرية، اختبارات شفهية، ومشاريع مع درجات مرصودة.
- **الامتحانات الرسمية**: امتحانات نصفية لمواد الرياضيات، الفيزياء، واللغة العربية مع نتائج وكشوف علامات.
- **الإنذارات**: إنذارات وملاحظات سلوكية واقعية.
- **المالية والرسوم**: أنواع رسوم، رسوم صفوف، منح تفوق وخصومات عائلية، وإيصالات دفع مسجلة.
- **المكتبة**: عناوين كتب مرجعية وأدبية وسجلات استعارة نشطة ومكتملة.
- **الإعلانات**: إعلانات ترحيبية، مواعيد امتحانات، مؤتمرات أولياء أمور، ومعارض علمية.
